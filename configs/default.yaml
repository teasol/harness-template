# Example experiment configuration. Rename and extend per project.
# This is *data*, not logic — keep control flow out of YAML.
experiment:
  name: baseline
  seed: 42

data:
  root: data/
  dataset: example
  version: v1

model:
  name: example-model
  params: {}

training:
  epochs: 10
  batch_size: 32
  learning_rate: 0.001

output:
  dir: results/
